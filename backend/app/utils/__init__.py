"""
The `utils` package.

This package contains various utility modules that provide helper functions,
decorators, custom exceptions, and other tools used across the application.
Making this directory a package allows for organized import of these utilities.
"""
# This file intentionally left not-quite-blank.
# It signifies that the 'utils' directory should be treated as a Python package,
# allowing modules within it to be imported using dot notation (e.g., from app.utils import decorators).